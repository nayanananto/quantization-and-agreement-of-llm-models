accelerate>=1.0,<2
bitsandbytes>=0.45,<1
datasets>=2.20,<5
numpy>=1.26,<3
sentencepiece>=0.2,<1
# Both architectures use native model implementations, so keep a modern,
# mutually compatible Transformers/Accelerate/bitsandbytes stack.
transformers>=4.56,<5
